<table>
  <caption><%= @author.name %> - Books</caption>
  <tr>
    <th>Book name</th>
  </tr>
  <% @author.books.each do |book| %>
    <tr>
      <td>
        <%= link_to book.title, book_path(book) %>
      </td>
    </tr>
  <% end %>
</table>
